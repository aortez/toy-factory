# Frozen dark-guard mortality audit

Follow the [64-day panel](renewal-dark-panel.md) using only its saved records.
Pin manifest `d62a870fea9ecfede3a32cebe1465097c0391f53a0cf781358574fc2db2e4ae8`
and portable-summary SHA-256
`acab8e8c821a11f9b8748bc30856c4af1a1c788efa7d5cb4f223650be80b9ca9`.
No native simulations, screenshots, training, C edits, rule changes, deployment,
commit or push. Do not alter the frozen bundle. Record this scope before analysis.

## Population and comparisons

Retain all sixteen trajectories in their declared order: four existing worlds,
control/guard, patch/no-patch. Primary subjects are **all guarded natural deaths**;
the other worlds and ordinary controls remain comparisons, not a new selection
or held-out test. Keep patch deaths separate. Highlight the already identified
`abf7af73` patched regression, without treating descendant IDs across arms as
matched individuals. Shared pre-patch histories and repeated denials are not
independent observations.

## Fixed measurements

- Re-verify the parent capture, source/model identities, raw repeats and saved
  analyses. Reconcile ordinary live resource/leaf/stress accounting, then carry
  post-patch state forward. No fabricated terminal-step income or expense.
- Audit **every living post-step phase-117 state** (the last possibly productive
  step before the guaranteed-dark interval), and every newborn's first state
  when already inside that interval. Include survivors and patch/trace-censored
  follow-ups, not just states belonging to later deaths. Forecast with the
  existing exact energy/stress reference; do not extrapolate daylight income.
- Follow each anchor until strictly before the next possibly productive step,
  or until natural death, patch death or trace end. Track optional expenses,
  energy-upkeep changes and maintenance water shortages as assumption breaks.
  Require exact energy/stress parity on the valid prefix and exact natural
  energy-death timing on complete clean windows. A patch is not starvation.
- For every natural death, classify its terminal shortage flags, location
  inside/after the latest observed dark window (or no preceding anchor), and
  that window's prediction/follow-up. Distinguish forecast-window survival from
  actual recovery afterward. Preserve the last live state and cleared terminal
  state separately.
- For every guarded death, retain lifetime spending totals and a fixed **two-day
  lookback**, clipped at birth. Reconcile its live budget and preserve accepted
  expense stages, body/upkeep changes, shortage/stress transitions and key light
  boundaries. Record actual income after the first possible-income boundary;
  never grant hypothetical light or water. Show the fixed-body dark budget at
  the storage cap and, if feasible, its minimum initial energy to survive the
  interval. This is arithmetic, not a proposed reserve rule or counterfactual
  rescue.
- Inventory all denials, separately identify already-fatal pre-expense budgets,
  and link them to observed same-case lifetimes. Count distinct plants as well
  as repeated attempts. An already-fatal denial is not an additional death or
  proof that the guard caused it.

Keep complete per-anchor/per-death results, per-case summaries and condition/arm
aggregates in a portable JSON. State terminal-budget, horizon, selection and
shared-history limitations. Repeat offline analysis exactly, fingerprint the
analysis sources, add boundary/censoring/corruption tests, and update issue 30,
the roadmap and experiment notes. Discuss the next proposal before any broader
guard or ecology intervention. Environment qualification remains open.
