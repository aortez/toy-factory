# Retry-aware FINISH — bounded protocol

Predeclared before native collection. Follow the failed one-shot FINISH veto,
not the successful extension arm. This is one selected-world causal diagnostic,
not a new general spending rule, controller, fitness function or training run.

## Frozen comparison and intervention

Parent bundle: `artifacts/garden-renewal-purchase-veto-v1`, manifest SHA-256
`4a2c01588a204bbd4794d55da6b16cbd96948c0827e7cb3e7339fbd82dea5e4d`.
Use its same `abf7af73.patch.guard` world, N background/all descendants
(`01b9d94a`), W founder 5 only (`c9ea07fd`), no-night-growth probe, selective
leaf renewal, 512 nodes, eight plant/seed slots, undrained headroom-v1/wide-v1,
no gardener, and patch seed `05d87ca0`. Keep `dark-spending-guard-v1` unchanged.

Three independently reset arms: control (hook disabled), the existing one-shot
`finish`, and `finish-retry`. No extension veto in this comparison.

The new arm rejects the same eligible winning FINISH by lineage **22**, tip
**271**, at both remaining uncovered daylight attempts:

| Tick / phase | Pre-purchase energy / water / nodes / stress | Cost |
| --- | --- | --- |
| 69885 / 115 | 92 / 152 / 21 / 0 | 9 energy / 5 water |
| 69900 / 116 | 90 / 161 / 21 / 0 | 9 energy / 5 water |

The second receipt is known from the previous one-shot trace: both new arms
share history up to that transaction. Both proposals leave nodes at 21.
The diagnostic stops **before tick 69915 / phase 117**, when the ordinary
forecast becomes supported. It neither forces a purchase nor suppresses future
guard-approved purchases. No reranking, alternative-tip action, refund or state
advance on rejection. Wrong/missing/out-of-order/repeated receipts fail the run.
Use separate intervention metadata; do not change ordinary guard counters.

## Fixed collection budget and verification

Freeze sources, protocol, exact command list, build cache/graph, models and
binaries before collection. All three arms run to **245760 ticks / 64 days**,
each repeated: six complete ecology traces. Replay each arm twice at **72960,
122880 and 245760** (days 19, 32, 64): eighteen replays, **24 native calls total**.
No extra native experimental runs, seeds, tuning, training or firmware deploy.
Unit/invalid-CLI fixtures and ordinary regression suites are separate from that
capture budget. Retain failed captures without silently rerunning them.

Require rebuilt control and one-shot FINISH traces and frames to reproduce the
parent exactly. The retry arm must preserve every raw record up to the first
purchase versus control and up to the second purchase versus one-shot FINISH,
ignoring only intervention metadata. Keep the rejected candidate bids and prove
both immediate cost/tip/private-state invariants. Count exactly two forced
vetoes, no more, and verify the tick-69915 handoff to the unchanged guard.

Require exact repeats, frame/census hash and framebuffer CRC agreement,
independent live resource/guard/stress/leaf/tip accounting, unchanged patch
schedule, and whole-world lineage/cohort reconciliation. Cleared terminal
resource telemetry is not reconstructed. Native rendering must not mutate state.

## Outcomes and stopping point

Follow flower 22's dark entry, later accepted or rejected FINISH/growth,
night survival, actual dawn recovery, death/censoring, seeds and offspring.
Retain all neighbor and whole-world effects through day 64: natural versus
patch mortality, germination, eligible/full-day offspring, durable parents,
species/family retention (including seed bank), living/tipless exposure and
the established **days 48–64 closing window**. Match pre-intervention identities
only; newborn IDs after divergence do not establish matched individuals.

Inspect all nine fixed native frames, including unchanged controls. A rescue
or failure in this selected case does not qualify a broad policy. Stop with a
report and next proposal; do not automatically extend the refusal interval,
change forecast horizon, tune the general guard, train, commit, push or deploy.
